Parent to child concept = Props
Child to parent = Lifting 

--- parent component bnao
import React, {useState} from 'react'
import Child from './Child'   // child inherit krna ha

const Props = () => {
    const [count, setCounter] = useState(0);
  return (
    <View>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}> Props </Text>
      <Button title="Counter" onPress={()=> setCounter(count+1)} />
      <Child data={count} />  // child ma variable pass kia ha 
    </View>
  )
}

--- Child component
const Child = (props) => {  // parameter ma value get ki
    console.log(props)
  return (
    <View>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}> Child </Text>
      <Text style={{ fontSize: 20, fontWeight: 'bold' }}> Prop Count {props.data} </Text> // yahn show krwadia  
    </View>
  )
}